<header>
  ⚔️
  <nav>
    <ul>
      {% for entry in collections.all | eleventyNavigation %}
      <li class="{% if entry.url == page.url %}is-active{% endif %}"><a href="{{ entry.url }}">{{ entry.title }}</a></li>
      {% endfor %}
    </ul>
  </nav>
</header>
